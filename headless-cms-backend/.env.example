HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Live preview (config/admin.ts) — PREVIEW_SECRET must match the same
# variable in next-headless-cms-fe/.env; PREVIEW_BASE_URL is that tenant
# frontend's origin (e.g. http://localhost:3002 in dev).
PREVIEW_BASE_URL=http://localhost:3002
PREVIEW_SECRET=tobemodified
