{
  "collectionName": "components_blocks_stacks",
  "info": {
    "displayName": "Stack",
    "icon": "layer",
    "description": "Vertical stack layout"
  },
  "options": {},
  "attributes": {
    "gap": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ]
    },
    "align": {
      "type": "enumeration",
      "enum": [
        "start",
        "center",
        "end",
        "stretch"
      ]
    },
    "fullWidth": {
      "type": "boolean",
      "default": false
    },
    "height": {
      "type": "string"
    },
    "maxWidth": {
      "type": "string"
    },
    "padding": {
      "type": "string"
    },
    "margin": {
      "type": "string"
    },
    "backgroundColor": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "border": {
      "type": "enumeration",
      "enum": [
        "none",
        "hairline",
        "invertedOutline"
      ]
    },
    "slots": {
      "type": "json"
    },
    "borderRadius": {
      "type": "string"
    },
    "overflow": {
      "type": "enumeration",
      "enum": [
        "visible",
        "hidden",
        "auto"
      ]
    }
  }
}
