{
  "collectionName": "components_blocks_texts",
  "info": {
    "displayName": "Text",
    "icon": "bold",
    "description": "Body / lead / caption / label leaf"
  },
  "options": {},
  "attributes": {
    "content": {
      "type": "text",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "body",
        "lead",
        "caption",
        "label"
      ],
      "default": "body"
    },
    "bold": {
      "type": "boolean",
      "default": false
    },
    "fullWidth": {
      "type": "boolean",
      "default": false
    },
    "height": {
      "type": "string"
    },
    "maxWidth": {
      "type": "string"
    },
    "padding": {
      "type": "string"
    },
    "margin": {
      "type": "string"
    },
    "backgroundColor": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "border": {
      "type": "enumeration",
      "enum": [
        "none",
        "hairline",
        "invertedOutline"
      ]
    },
    "dividerTop": {
      "type": "boolean",
      "default": false
    },
    "borderRadius": {
      "type": "string"
    },
    "overflow": {
      "type": "enumeration",
      "enum": [
        "visible",
        "hidden",
        "auto"
      ]
    },
    "fontSize": {
      "type": "enumeration",
      "enum": [
        "cardTitle",
        "sectionTitle",
        "priceCompact",
        "pageTitle",
        "price",
        "display",
        "statement",
        "custom"
      ]
    },
    "customFontSize": {
      "type": "string"
    },
    "fontWeight": {
      "type": "string"
    },
    "textAlign": {
      "type": "enumeration",
      "enum": [
        "left",
        "center",
        "right"
      ]
    }
  }
}
