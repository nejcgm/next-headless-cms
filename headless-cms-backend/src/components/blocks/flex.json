{
  "collectionName": "components_blocks_flexes",
  "info": {
    "displayName": "Flex",
    "icon": "arrows",
    "description": "Flex layout"
  },
  "options": {},
  "attributes": {
    "direction": {
      "type": "enumeration",
      "enum": [
        "row",
        "column"
      ],
      "default": "row"
    },
    "gap": {
      "type": "enumeration",
      "enum": [
        "sm",
        "md",
        "lg"
      ]
    },
    "align": {
      "type": "enumeration",
      "enum": [
        "start",
        "center",
        "end",
        "stretch"
      ]
    },
    "justify": {
      "type": "enumeration",
      "enum": [
        "start",
        "center",
        "end",
        "between"
      ]
    },
    "wrap": {
      "type": "boolean",
      "default": false
    },
    "fullWidth": {
      "type": "boolean",
      "default": false
    },
    "height": {
      "type": "string"
    },
    "maxWidth": {
      "type": "string"
    },
    "padding": {
      "type": "string"
    },
    "margin": {
      "type": "string"
    },
    "backgroundColor": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "border": {
      "type": "enumeration",
      "enum": [
        "none",
        "hairline",
        "invertedOutline"
      ]
    },
    "slots": {
      "type": "json"
    },
    "borderRadius": {
      "type": "string"
    },
    "overflow": {
      "type": "enumeration",
      "enum": [
        "visible",
        "hidden",
        "auto"
      ]
    }
  }
}
