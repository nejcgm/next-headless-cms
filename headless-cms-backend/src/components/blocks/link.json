{
  "collectionName": "components_blocks_links",
  "info": {
    "displayName": "Link",
    "icon": "link",
    "description": "Text link leaf (primary or muted)"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "href": {
      "type": "string",
      "required": true
    },
    "variant": {
      "type": "enumeration",
      "enum": [
        "primary",
        "muted"
      ],
      "default": "primary"
    },
    "showArrow": {
      "type": "boolean",
      "default": false
    },
    "fullWidth": {
      "type": "boolean",
      "default": false
    },
    "height": {
      "type": "string"
    },
    "maxWidth": {
      "type": "string"
    },
    "padding": {
      "type": "string"
    },
    "margin": {
      "type": "string"
    },
    "backgroundColor": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "border": {
      "type": "enumeration",
      "enum": [
        "none",
        "hairline",
        "invertedOutline"
      ]
    },
    "borderRadius": {
      "type": "string"
    },
    "overflow": {
      "type": "enumeration",
      "enum": [
        "visible",
        "hidden",
        "auto"
      ]
    },
    "fontSize": {
      "type": "string"
    },
    "fontWeight": {
      "type": "string"
    },
    "textAlign": {
      "type": "enumeration",
      "enum": [
        "left",
        "center",
        "right"
      ]
    }
  }
}
