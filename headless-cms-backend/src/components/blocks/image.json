{
  "collectionName": "components_blocks_images",
  "info": {
    "displayName": "Image",
    "icon": "picture",
    "description": "Image leaf"
  },
  "options": {},
  "attributes": {
    "src": {
      "type": "string",
      "required": true
    },
    "alt": {
      "type": "string"
    },
    "fit": {
      "type": "enumeration",
      "enum": [
        "cover",
        "contain"
      ],
      "default": "cover"
    },
    "fullWidth": {
      "type": "boolean",
      "default": false
    },
    "height": {
      "type": "string"
    },
    "maxWidth": {
      "type": "string"
    },
    "padding": {
      "type": "string"
    },
    "margin": {
      "type": "string"
    },
    "backgroundColor": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "accent",
        "background",
        "foreground",
        "muted",
        "border",
        "text-primary"
      ]
    },
    "border": {
      "type": "enumeration",
      "enum": [
        "none",
        "hairline",
        "invertedOutline"
      ]
    },
    "borderRadius": {
      "type": "string"
    },
    "overflow": {
      "type": "enumeration",
      "enum": [
        "visible",
        "hidden",
        "auto"
      ]
    }
  }
}
