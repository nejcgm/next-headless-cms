---
description: Spec Kit analyze (alias for /speckit-analyze)
argument-hint: "Follows .claude/skills/speckit-analyze/SKILL.md"
---

Read and execute `.claude/skills/speckit-analyze/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
