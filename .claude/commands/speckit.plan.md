---
description: Spec Kit plan (alias for /speckit-plan)
argument-hint: "Follows .claude/skills/speckit-plan/SKILL.md"
---

Read and execute `.claude/skills/speckit-plan/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
