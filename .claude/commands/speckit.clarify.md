---
description: Spec Kit clarify (alias for /speckit-clarify)
argument-hint: "Follows .claude/skills/speckit-clarify/SKILL.md"
---

Read and execute `.claude/skills/speckit-clarify/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
