---
description: Spec Kit converge (alias for /speckit-converge)
argument-hint: "Follows .claude/skills/speckit-converge/SKILL.md"
---

Read and execute `.claude/skills/speckit-converge/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
