---
description: Spec Kit checklist (alias for /speckit-checklist)
argument-hint: "Follows .claude/skills/speckit-checklist/SKILL.md"
---

Read and execute `.claude/skills/speckit-checklist/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
