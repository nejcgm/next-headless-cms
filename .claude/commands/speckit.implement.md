---
description: Spec Kit implement (alias for /speckit-implement)
argument-hint: "Follows .claude/skills/speckit-implement/SKILL.md"
---

Read and execute `.claude/skills/speckit-implement/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
