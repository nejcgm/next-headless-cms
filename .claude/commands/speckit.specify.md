---
description: Spec Kit specify (alias for /speckit-specify)
argument-hint: "Follows .claude/skills/speckit-specify/SKILL.md"
---

Read and execute `.claude/skills/speckit-specify/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
