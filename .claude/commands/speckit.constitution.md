---
description: Spec Kit constitution (alias for /speckit-constitution)
argument-hint: "Follows .claude/skills/speckit-constitution/SKILL.md"
---

Read and execute `.claude/skills/speckit-constitution/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
