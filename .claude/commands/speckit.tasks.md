---
description: Spec Kit tasks (alias for /speckit-tasks)
argument-hint: "Follows .claude/skills/speckit-tasks/SKILL.md"
---

Read and execute `.claude/skills/speckit-tasks/SKILL.md` now. Treat the following as `$ARGUMENTS` for that skill:

```text
$ARGUMENTS
```
