// Initialize core systems — import this in the root layout
// Only the selected tenant's blocks are included (TENANT_ID build-time alias)

import "@shared/components/blocks";
import "@tenant/blocks";

export {};
